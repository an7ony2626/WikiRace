# Password for the local PostgreSQL container (any value).
DB_PASSWORD=changeme

# Must be Base64 and decode to at least 32 bytes: replace it with the
# output of `openssl rand -base64 48` (see README). The backend refuses
# to start while this placeholder is still here.
JWT_KEY=REPLACE_ME_WITH_OUTPUT_OF_openssl_rand_base64_48
